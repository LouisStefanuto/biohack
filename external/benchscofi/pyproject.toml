[tool.poetry]
name = "benchscofi"
version = "0.1.0"
description = ""
authors = ["Julien Blanchon <julien@blanchon.cc>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^2.1.3"
scipy = "^1.14.1"
pandas = "^2.2.3"
stanscofi = { path = "../stanscofi", develop = true }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
